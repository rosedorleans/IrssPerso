{% extends 'base.html.twig' %}

{% block title %}Calendar{% endblock %}

{% block body %}

    <div class="container">
        <h1>Filtrer</h1>
        <div class="grid">
            {% for event in allEvents %}
                {% include 'pages/_card.html.twig' with {event: event} only %}
            {% endfor %}
        </div>
    </div>

    <a href="{{ path('event_index') }}">Retour aux dates clés</a>


{% endblock %}
